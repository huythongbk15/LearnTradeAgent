name: Phase 6 Multi-Region & Integration

on:
  push:
    branches: [main]
    paths:
      - 'trading/**'
      - 'tests/**'
      - 'scripts/**'
  pull_request:
    paths:
      - 'trading/**'
      - 'tests/**'
      - 'scripts/**'
  workflow_dispatch:

jobs:
  integration:
    name: Integration Tests (dry-run)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('pyproject.toml') }}

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install pytest

      - name: Run full test suite
        run: |
          python -m pytest tests/ test_phase6.py -q

      - name: Verify multi-region dry-run (no cluster)
        run: |
          timeout 15 python trading/infrastructure/multi_region/sync_controller.py dryrun

      - name: Verify chaos dry-run (no cluster)
        run: |
          python scripts/chaos_dryrun.py | head -20

  loadtest:
    name: Load Tests (quick)
    runs-on: ubuntu-latest
    needs: integration
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install pytest

      - name: Run quick load tests
        run: |
          python scripts/load_test_phase6.py --quick

      - name: Run benchmarks
        run: |
          python scripts/benchmark_phase6.py

  multi-region-manifest:
    name: Validate Multi-Region Manifests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate sync controller imports
        run: |
          python -c "from trading.infrastructure.multi_region.sync_controller import RegionSyncController, RegionInfo, RegionRole, SyncPolicy; print('multi-region imports OK')"

      - name: Validate chaos imports
        run: |
          python -c "from trading.infrastructure.chaos.chaos_experiments import ChaosExperimentSuite, ChaosExperimentType; print('chaos imports OK')"
